.DS_Store
.clawhub/
.openclaw/
_meta.json

# Local data — never commit to skill repo
# Data lives in ~/.openclaw/workspace/peeps/ (outside the skill folder)
# and should never be mixed with skill code
data/
/peeps/
kyp/
contacts/
people/
*.db
*.sqlite
